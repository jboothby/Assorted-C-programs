/*	This program returns the number of files under the current
 *	location in the file structure that are readable by the
 *	current process
*/
#ifndef ASSIGNMENT3_H
#define	ASSIGNMENT3_H
int readable(char *inputPath);
int enumFiles(char *inputPath);
#endif
